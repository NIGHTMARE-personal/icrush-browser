import React from 'react';

interface BookmarkToolbarProps {
  onNavigate: (url: string) => void;
  onOpenBookmarks: () => void;
}

export function BookmarkToolbar(_props: BookmarkToolbarProps) {
  return null;
}
